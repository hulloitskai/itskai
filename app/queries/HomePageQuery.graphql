query HomePageQuery($entryId: ID) {
  entry: homepageJournalEntry(id: $entryId) {
    ...HomePageJournalEntryEntryFragment
    id
  }
  location {
    approximateAddress
    googleMapsAreaUrl
    id
    timestamp
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
