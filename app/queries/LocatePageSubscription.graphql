subscription LocatePageSubscription($accessToken: String!) {
  location {
    details(accessToken: $accessToken) {
      address
      coordinates {
        latitude
        longitude
      }
      expiresAt
      trail {
        id
        ...LocatePageTrailMarkerFragment
      }
    }
    id
    timestamp
  }
}
