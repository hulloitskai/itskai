query JournalEntryCommentsQuery($entryId: String!, $startCursor: String) {
  comments: journalEntryComments(
    entryId: $entryId
    startCursor: $startCursor
    pageSize: 100
  ) {
    items {
      id
      createdAt
      richText
    }
    nextCursor
  }
}
