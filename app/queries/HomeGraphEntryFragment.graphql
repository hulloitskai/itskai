fragment HomeGraphEntryFragment on ObsidianEntry {
  id
  type: __typename
  name
  referencedBy {
    id
  }
  ... on ObsidianNote {
    modifiedAt
    aliases
    tags
    references {
      id
    }
    blurb
  }
}
