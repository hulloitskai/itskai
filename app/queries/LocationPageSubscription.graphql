subscription LocationPageSubscription($password: String!) {
  location {
    details(password: $password) {
      address
      coordinates {
        latitude
        longitude
      }
      expiresAt
      trail {
        latitude
        longitude
      }
    }
    id
    timestamp
  }
}
