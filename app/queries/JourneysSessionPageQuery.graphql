query JourneysSessionPageQuery($sessionId: ID!) {
  session: journeysSession(id: $sessionId) {
    id
    participations {
      goal
      id
      participantIsViewer
      participantName
    }
    startedAt
  }
  viewer {
    ...AppViewerFragment
    id
  }
}
