query NotionEntryCommentsQuery($pageId: String!, $startCursor: String) {
  comments: notionComments(
    blockId: $pageId
    startCursor: $startCursor
    pageSize: 100
  ) {
    items {
      id
      createdAt
      richText
    }
    nextCursor
  }
}
