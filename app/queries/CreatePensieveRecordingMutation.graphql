mutation CreatePensieveRecordingMutation(
  $input: CreatePensieveRecordingInput!
) {
  payload: createPensieveRecording(input: $input) {
    recording {
      audio {
        id
        url
      }
      id
    }
  }
}
